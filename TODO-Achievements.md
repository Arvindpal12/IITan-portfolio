# TODO-Achievements

- [x] Located achievements bullet-point rendering in `src/components/Achievements/Achievements.jsx`.
- [ ] Replace `<ul>` / `<li>` + `ach.points.map(...)` with a single `<p>` paragraph per achievement card.
- [x] Rewrite the existing point content into a flowing professional paragraph per card (no bullet formatting).

- [x] Ensure layout/styles remain consistent and gallery modal behavior is unchanged.
- [x] Run `npm run build` (or `npm run lint` if available) to verify no build errors.


